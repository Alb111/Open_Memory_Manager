# Debug scan-chain architecture

The scan-chain documentation now lives in **[docs/DFT-and-Scan.md](docs/DFT-and-Scan.md)**
— see the "OMM Scan Chains" section for the external pad interface, the debug-mode
distribution, the per-chain register order (Chains 0–3), the synchronizer / SRAM-macro
exclusions, and the scan/functional STA timing modes.

This file is kept only as a pointer so existing links do not break; do not add
scan details here — update `docs/DFT-and-Scan.md` instead.
